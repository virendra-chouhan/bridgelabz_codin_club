1.Listing all files of the Directory.

2.find the extensions of the files.
example : .py, .js, .yml

3.create a folder in backups directory with same name as the extension's names of files.
example :  py/ js/ yml/

4.move original files into same name's of extension's folder.